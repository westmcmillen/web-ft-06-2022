console.log( "Hello" );

const restaurants = [ "Carmelita's", "Jose's", "Municipal Taco" ];

// console.log( restaurants[ restaurants.length - 1 ] );
// console.log( restaurants.at( -1 ) );

for ( let restaurant of restaurants ) {
    console.log( restaurant );
}